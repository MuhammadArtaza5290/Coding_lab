 <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body class="min-h-screen bg-zinc-900  ">
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <a class="text-blue-500" href="/">Back To Home</a>
        <h1 class="text-3xl mt-10 tracking-tighter mb-3">All user</h1>
        <div class="users flex gap-3 flex-wrap">
            <% if(user.length > 0){ %>
                <% user.forEach((val)=>{ %>
            <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                <div class="w-full h-72 bg-zinc-600 rounded-lg overflow-hidden">
                    <img class="w-full h-full object-cover object-center" src="<%= val.imgUrl %>" alt="">
                </div>
                <h3 class="text-xl mt-3 tracking-tighter"><%= val.name %></h3>
                <h5 class="text-zinc-500"><%= val.email %></h5>
                <div class="flex gap-3 mt-10">
                    <a href="/edit/<%= val._id %>" class="text-green-600">Edit User</a>
                    <a href="/delete/<%= val._id %>" class="text-red-500">Delete User</a>
                </div>
            </div>
            <% }) %>
            <% } else {%> 
                <h1 class="text-zinc-500 text-3xl">No User yet</h1>
                <% } %>
        </div>
    </div>
</body>
</html>